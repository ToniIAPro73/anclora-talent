# Dashboard empty state

## Intent
Keep the empty dashboard invitation visually consistent with the header action while making the first-project entry point quiet and editorial.

## Acceptance criteria
- The empty-state CTA uses the same text-only primary button treatment as the dashboard header action.
- The CTA keeps the existing `/projects/new` route and localized label.
- The empty state remains readable and useful when no projects exist.
