# Plan

1. Extend the shared font selector to include the current family and render family names with their own CSS family.
2. Replace the document modal's native visual control with the shared selector while retaining a synchronized form-compatible select.
3. Add focused tests and run the relevant suite, lint, and build.
