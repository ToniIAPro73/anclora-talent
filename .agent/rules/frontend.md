# Frontend Rules

- Organize code by feature, not by page mock size.
- Keep application shell concerns in `src/app`.
- Keep reusable primitives in `src/shared`.
- Avoid monolithic files over time; split once a file becomes hard to reason about.
- Prefer typed props and exported domain types.
- UI components should support real data flows, not only decorative placeholder content.
