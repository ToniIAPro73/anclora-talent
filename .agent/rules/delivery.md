# Delivery Rules

- Build and typecheck must pass after each substantial change.
- Prioritize vertical slices over disconnected visual polish.
- Features must be introduced in MVP order: project -> import -> edit -> preview -> export.
- AI functionality should not lead the architecture; it should enhance stable flows.
- Documentation in `sdd` must be updated when the data model or scope changes materially.
